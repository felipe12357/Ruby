
<%= link_to "Create new",new_article_f_path %>

All articles Fs:<br>

<div id="articles">
  <table>
    <thead>
      <th>Title</th>
      <th>Description </th>
      <th>Author</th>
     <th colspan="3">Actions</th>
    </thead>
    <% @articleFs.each do |article| %>
      <tr>
        <td><%=article.title %></td>
        <td><%=article.description %></td>
        <td><%= article.author %></td>
        <td><%=  link_to "show",article %></td>
        <td> <%= link_to "edit", edit_article_f_path(article) %> </td>
        <!--  se puede de cualquiera de las 3 formas -->
       <%#  <td> <%= link_to "Delete",article, data: { turbo_method: :delete } </td> %>
         <td> <%= link_to "Delete",article_f_path(article), data: { turbo_confirm:"Are you sure?", turbo_method: :delete } %></td>
        <%#  <td> <%= link_to "Delete","#{article_fs_path}/#{article.id}", data: { turbo_method: :delete } </td> %>
      </tr>
    <% end %>
  </table>
</div>